# 진동 측정 앱 — 에이전트 작업 규칙

이 프로젝트에서 코드를 생성/수정할 때 아래 규칙을 항상 따른다.

## 최상위 기준
- **모든 판단 및 개발의 유일한 기준 문서는 루트의 `integration_plan.md` 이다.**

## 불변 규칙 (위반 금지)
1. **골든 픽스처 불변**: `assets/sample/2024F1447R01.txt`는 골든 픽스처다. 절대 수정·삭제·재생성 금지.
2. **파라미터 단일 정의**: 지표·필터·임계 수치는 `lib/domain/measure/metrics_config.dart` 한 곳에만 존재해야 한다. 다른 파일에 수치 리터럴로 복제 금지.
3. **임계 판정 불변**: X>10, Y>10, Z>15 mg, 소음>50 dBA (`integration_plan.md` D3). 변경 금지.
4. **디자인 규칙 준수**: 본 문서의 디자인 규칙(토큰 전용, 터치 56dp, 주 버튼 64dp 1개, 색+아이콘+텍스트 3중 표시, 본문 18sp+, 한국어 주석/문구)은 신규·수정 코드 전체에 적용된다.
5. **요구사항 엄수**: 요구사항 6개(`integration_plan.md` D11~D14 근거)는 축소·재해석 금지. 문서에 없는 기능 추가도 금지(단순할수록 좋다는 원칙 D10).

## 프로젝트 컨텍스트
- OTIS 승강기 설치 현장의 진동·소음 측정 앱. 사용자는 나이가 있는 현장 작업자.
- 화면 6개 + bottom sheet 2개 구조. 라우트는 `lib/core/router.dart` 참조.
- 출력 기준: 기존 TUNE 리포트 (X/Y/Z 진동 P2P, 소음 최대, 운행거리, 최대속도 / 임계: X·Y>10mg, Z>15mg, 소음>50dB → 빨강)
- 샘플 raw 데이터: `assets/sample/2024F1447R01.txt` (EVIMP1 헤더, 256Hz, 4컬럼: X,Y,Z 진동 mg + 소음 dBA)

## 디자인 규칙 (위반 금지)
1. **색·치수·타이포 하드코딩 금지.** 반드시 `lib/core/theme.dart`의 `AppColors` / `AppDims` / `AppText`만 참조. 새 토큰이 필요하면 theme.dart에 추가하고 사용.
2. **어르신 UX**: 터치 타깃 최소 56dp(`AppDims.touchMin`), 주 버튼 높이 64dp full-width, 본문 18sp 미만 금지(캡션 최소 16).
3. **상태 표시는 3중**: 색 + 아이콘 + 텍스트. 색만으로 정보 전달 금지. (예: 기준 초과 = 빨강 + 경고 아이콘 + "기준 초과" 텍스트)
4. **금지**: 그라디언트, 이모지, glassmorphism, 과한 그림자(elevation 2 초과), pill 남용. radius는 `AppDims.radius`(12) 고정.
5. 한 화면에 주 행동(ElevatedButton)은 1개. 제스처 전용 인터랙션(스와이프만 가능한 기능) 금지.
6. mock/예시 데이터는 실제 값 사용: 제번 `2024F 1447R01`, 현장 `럭키종합건설/송정동근생`, 1층→8층, X 8.2mg / Y 12.9mg / Z 22.2mg / 소음 71.7dBA / 20.0m / 1.50m/s.

## 코드 규칙
- 폴더: `lib/features/<기능>/` 단위. 공용 위젯은 `lib/features/shared/`.
- 라우팅은 go_router (`lib/core/router.dart`). 새 화면 구현 시 해당 라우트의 `_Todo`를 교체.
- 상태관리: 화면 로컬은 StatefulWidget으로 충분. 전역 상태가 필요해지기 전까지 외부 상태관리 패키지 도입 금지.
- 측정/연산 로직은 UI와 분리: `lib/domain/` 에 순수 Dart로 작성하고 유닛테스트를 붙인다. (A95 산출, 적분, 임계 판정 등)
- 주석과 UI 문자열은 한국어.
- 기존 화면(login_screen.dart)의 스타일·구조·네이밍을 따른다.
