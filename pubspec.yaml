name: vibration_checker
description: OTIS 승강기 진동·소음 측정 및 TUNE 리포트 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_email_sender: ^10.0.1
  go_router: ^17.3.0
  intl: ^0.20.3
  path_provider: ^2.1.6
  pdf: ^3.12.0
  sensors_plus: ^7.1.0
  shared_preferences: ^2.5.5
  wakelock_plus: ^1.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sample/
    - assets/fonts/

