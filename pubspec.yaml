name: vibration_checker
description: OTIS 승강기 진동·소음 측정 및 TUNE 리포트 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  go_router: ^14.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sample/

