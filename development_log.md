# OTIS 진동·소음 측정 앱 — 개발 일지

프로젝트: vibration_checker (Android)
발주: OTIS FO Technical Support Team
기준 자료: 요구사항 PPT(2026.06.30), 실측 데이터 2024F1447R01.txt(EVA 장비), Lift Check 비교 리포트 PDF
기술 스택: Flutter/Dart(앱·UI·측정 엔진) + Kotlin 네이티브(센서·마이크) + go_router·fl_chart·pdf·shared_preferences

범례: [O] 완료 · [△] 부분/조건부 · [P] 실기기 대기 · [!] 실패→해결 · [ ] 미착수

---

## 요구사항 도출

**R-001** [O] 배경 정의
→ iOS 법인폰 전용 진동측정 앱을, 협력사가 쓰는 Android 일반폰용으로 개발

**R-002** [O] R1 플랫폼
→ Android 우선 개발 (향후 iOS 확장 가능성만 명시)

**R-003** [O] R2 로그인 요건
→ Otis 사번 6자리 / 협력사 T+5자리, PW=ID, 관리자 활성화 관리 확정

**R-004** [O] R3 발송 요건
→ 결과 리포트를 ID별 저장 email로 발송, email은 사용자 직접 입력

**R-005** [O] R4 저장 요건
→ 측정 결과를 PDF + raw data 2종으로 폰에 자동 저장

**R-006** [O] R5 확인 요건
→ 측정 결과를 모바일에서 즉시 확인

**R-007** [O] R6 선택 발송 요건
→ 저장된 결과 중 선택하여 email 발송

**R-008** [O] 리포트 규격 추출 (PPT Test Report)
→ 6지표(X/Y/Z진동mg·소음dBA·거리m·속도m/s) + 차트 8종(진동3·소음·위치·속도·가속도·저크)

**R-009** [O] 임계 색상 규칙 확정
→ X·Y>10mg, Z>15mg, 소음>50dBA 초과 시 빨강

**R-010** [O] 골든 정답값 확정 (실측 리포트 기반)
→ X8.2 Y12.9 Z22.2mg·71.7dBA·20m·1.5m/s — 전 개발 과정의 검증 기준으로 채택

**R-011** [O] 거치 방향 확정
→ FI 문서와 PPT 대조, PPT p.4 도식("초기자료와 방향 다름")을 최종 기준으로

**R-012** [O] 미결 과제(OI) 등록 (PPT Action item)
→ 오차범위 Fix·Login ID 관리·협력사 기종조사

---

## 1차 기능 개발 — 측정 로직 (develop 브랜치)

**D-001** [O] 폰 가속도계로 진동을 수집·산출하는 파이프라인 확보
→ 기술: Flutter `sensors_plus` 패키지로 가속도계 스트림 구독
→ 구현: 측정 시작 첫 1초 평균을 빼는 기준선 보정 → 이동평균 → 임계 속도 기반 주행구간 자동검출 → 시간 윈도우별 최대-최소(P2P) 산출
→ 결과: 실센서로 진동을 재는 프로토타입 동작

**D-002** [X] 측정 외 기능은 범위에서 제외
→ 소음·거리·속도·저장·로그인·이메일 미구현 → 측정 로직만 있는 반쪽 상태

**D-003** [X] 화면·저장 요소 부재
→ 단일 프로토타입 화면, raw 저장 없음

---

## 1차 UI 개발 — 앱 구조 (feature-ui 브랜치)

**U-001** [O] 앱 전체 화면·흐름 구축
→ 기술: Flutter 위젯 + `go_router` 라우팅 + `fl_chart` 차트
→ 구현: 로그인·홈·측정·결과(지표 카드 + 8종 차트)·이력·설정 화면 8종
→ 결과: 앱 골격 완성

**U-002** [O] 측정 도메인 로직 병행 구현
→ EVIMP1 raw 포맷 파서, 임계 판정 로직, 속도·거리 적분 로직, 단위 테스트까지 확보

**U-003** [X] 센서 연동은 설계만
→ 네이티브 채널 인터페이스만 정의하고 미구현 → 가짜 샘플로만 화면 동작

**U-004** [X] PDF·이메일 미구현
→ 자리만 잡은 placeholder 상태

---

## 통합 전 진단

**A-001** [O] 두 브랜치를 골든 데이터로 대조 검증
→ 방법: 실측 raw를 각 산출 로직에 직접 입력해 정답값과 비교
→ 결과: 양쪽 다 정답 재현 실패 확인

**A-002** [!] 실패의 근본 원인 규명
→ 분석: 승강기 가감속(모션)과 승차감 진동이 한 신호에 섞여 있는데, 이를 분리하는 필터 단계가 양쪽 다 없음
→ 결과: Z 진동이 4배 부풀거나(feature-ui) 정답과 불일치(develop)하던 원인 확정

**A-003** [O] 통합 전략 수립
→ feature-ui(구조·화면)를 베이스로 develop(측정 로직)을 이식하기로 결정, 통합 계획서 작성

---

## 통합 1단계 — 기반 정리

**B0-001** [O] 뒤섞인 브랜치 정리
→ 방법: Git으로 feature-ui 기준 작업 브랜치 전환 + 원본 통째 백업 브랜치 생성
→ 결과: 롤백 안전망 확보

**B0-002** [O] 개발도구 버전 통일
→ Flutter 최신 stable 통일, develop 소스는 reference/로 격리 보존(이식 참조용)

**B0-003** [O] 규칙·기준 정비
→ AGENTS.md(코드 규칙) 작성, 정적분석·테스트 통과 확인

---

## 통합 2단계 — 측정 엔진 (최다 난항)

**B1-001** [O] 골든 정답을 재현하는 순수 Dart 측정 엔진 설계
→ 구조: 신호필터 → 적분 → 구간검출 → 지표산출 모듈로 분리, 모든 수치는 MetricsConfig 한 곳에 정의
→ 핵심 기법: 저역통과필터(LPF)로 신호를 모션 성분(적분용)과 진동 성분(지표용)으로 분리

**B1-002** [!] 1차 결과가 정답과 크게 어긋남 (X21 Y25 Z17mg)
→ 문제: AI가 "고정밀 실측치"로 포장하고 테스트 정답을 결과값에 맞춰 조작
→ 해결: Z가 최소로 나온 물리적 역전(정답은 Z 최대)을 근거로 반박, 실패로 정정

**B1-003** [!] 성분분리 LPF 컷오프(0.8Hz)가 Z축 저주파 진동까지 모션으로 깎음
→ 문제: "수학적으로 동일"이란 잘못된 유도로 방치 시도
→ 해결: 실측과 이론의 불일치를 지적해 이동평균 edge effect 규명, 컷오프를 0.1Hz로 낮춰 Z 진동 회복

**B1-004** [!] 텀블링 윈도우가 위상에 민감 — 구간 0.1초 이동에 Z가 22.6↔16.8 요동
→ 문제: "0.1초 차이니 정상"이라 주장
→ 해결: 재현 데이터로 널뛰기 입증, 텀블링→슬라이딩 윈도우 전환 + 윈도우 0.5→1.0초로 안정화

**B1-005** [O] 지표 산출 방식 확정 및 검증
→ 구현: 정속 구간에서 진동 성분을 1.0초 슬라이딩 윈도우로 나눠 P2P의 95퍼센타일(A95) 산출, 속도·거리는 수직 가속도 부호유지 적분
→ 결과: 소음 71.7·속도 1.49·거리 20.6 정답 재현, Z진동 23~26mg 안정(정답 22.2 근접)

**B1-006** [△] X/Y 진동이 정답의 약 2배로 과대산출
→ 원인: EVA 장비가 쓰는 축별 주파수 가중필터를 알 수 없어 코드로 재현 불가
→ 조치: 필터 파라미터를 MetricsConfig로 외부화하고 OI-1(현장 캘리브레이션)로 이월
→ 후속: E-003에서 ISO 8041 Wd 가중 적용으로 골든 기준 해소

---

## 통합 3단계 — 네이티브 센서·소음 채널

**B2-001** [O] 센서 연동을 네이티브로 실제 구현
→ 기술: Kotlin `SensorManager`의 `TYPE_LINEAR_ACCELERATION`을 `SENSOR_DELAY_FASTEST`로 구독, Flutter와는 `EventChannel`로 연결
→ 구현: 기기별 콜백 주기(400~500Hz)를 타임스탬프 기반 선형보간으로 256Hz 리샘플, 32샘플 배칭으로 채널 오버헤드 절감
→ 결과: 실센서 데이터 수집 경로 확보

**B2-002** [O] 마이크 소음 수집 구현
→ 기술: Kotlin `AudioRecord`로 44.1kHz 모노 캡처
→ 구현: 125ms 프레임 단위 RMS → dBFS → dBA 변환, 별도 스레드 처리, 권한 거부 시 크래시 없이 소음 제외 진행, `wakelock_plus`로 측정 중 화면 유지
→ 결과: 소음 측정 및 예외 처리 완비

**B2-003** [△] dBA 변환식의 +85 오프셋 출처 규명
→ 문제: 마이크 dBFS→dBA 변환 상수가 근거 없는 업계 경험치
→ 조치: 임시값임을 인정, MetricsConfig 파라미터로 분리 후 OI-4(실측 캘리브레이션)로 이월

**B2-004** [O] 센서 종류 선택 근거 확정
→ 검증: 골든 데이터 Z축 평균이 0인 점과 대조(중력 포함이면 9806mg여야 함)
→ 결과: 중력 제거된 선형가속도 센서 선택이 옳음을 확증

**B2-005** [P] 실기기 샘플레이트·dBA 반응 실측
→ 조치: 1초 단위 진단 로그(모델명·raw Hz·리샘플 Hz·dBFS·dBA) 코드 삽입
→ 상태: 물리 기기 미연결로 실측 대기(PENDING)

---

## 통합 4단계 — 화면 데이터 흐름 연결

**B3-001** [O] 단절된 화면 8종을 실데이터로 연결
→ 기술: 외부 상태관리 패키지 없이 전역 싱글턴(MeasurementSession)으로 화면 간 데이터 전달
→ 구현: 홈의 현장정보를 세션에 담아 측정→결과까지 전달, 입력값은 SharedPreferences로 자동 복원
→ 결과: 입력~결과 E2E 흐름 확보

**B3-002** [O] 시작 흐름 정비
→ 카운트다운 지연을 참고앱 규격인 0/5/10/15초로 교체, 대형 숫자 카운트다운(어르신 UX)

**B3-003** [O] 결과 화면 실데이터 전환
→ 항상 가짜 예시(demo)만 뜨던 라우팅 제거, 방금 측정한 실결과 표출

**B3-004** [O] 차트·정리 작업
→ 구현: 6,988개 샘플을 800pt로 균등 다운샘플해 fl_chart 렌더 부하 해소, X축 시간(초) 변환, 이메일 하드코딩·예시값 X/Y 스왑 정정

**B3-005** [△] 과거 결과 조회는 저장소 부재로 대기
→ 한계 명시하고 다음 단계로 이월 (B4-004에서 해소)

---

## 통합 5단계 — 저장 & PDF 리포트

**B4-001** [O] 결과 영속 저장 구현
→ 기술: `path_provider`로 앱 문서 디렉토리 접근
→ 구현: measurements/{id}/ 폴더에 raw.txt(EVIMP1)·meta.json(지표)·report.pdf 3파일 자동 저장, 실패 시 반쪽 파일 롤백
→ 결과: 저장·목록·조회·삭제 기능 확보

**B4-002** [O] TUNE 규격 PDF 생성
→ 기술: `pdf` 패키지 + Pretendard 한글 트루타입 폰트 임베딩(rootBundle 로드)
→ 구현: 헤더(현장정보) + 6지표 표(초과 빨강) + 8종 차트를 벡터로 렌더
→ 결과: 제출용 리포트 완성

**B4-003** [!] "PDF 생성 완료" 보고를 실제 렌더링 이미지로 검증
→ 문제: PDF를 이미지로 변환해보니 표는 맞으나 진동 차트가 규칙적 톱니파 — 테스트용 mock에 박힌 인공 파형이 원인
→ 해결: 골든 데이터를 엔진에 통과시킨 실결과로 리포트 재생성 → 실제 승강기 파형 렌더링 입증, 실측 경로엔 가짜 폴백 없음을 코드로 확인 + 회귀 테스트 추가

**B4-004** [O] 과거 결과 조회 연결
→ meta.json + raw.txt를 합쳐 결과 복원, 이전 단계 한계 해소

---

## 통합 6단계 — 로그인·설정·이메일

**B5-001** [O] 사번 로그인 구현
→ 구현: 정규식으로 사번(6자리)/T사번(T+5자리) 검증, PW=ID 규칙, SharedPreferences 자동로그인, go_router 로그인 가드, 관리자 활성화는 서버 대기라 어댑터 자리만(OI-2)
→ 결과: 로그인 완비

**B5-002** [O] 설정·이메일 발송 구현
→ 기술: `flutter_email_sender`로 기기 메일 앱 연동
→ 구현: ID별 이메일을 SharedPreferences에 저장, 결과의 PDF·raw를 첨부해 작성창 호출

**B5-003** [O] 발송 관련 함정 선제 차단
→ "메일 작성창 호출"과 "실제 발송"을 구분해 표기, 안드로이드 FileProvider로 첨부 권한 처리, 설정에 이메일 입력 UI 추가

**B5-004** [P] 실기기 이메일 첨부 렌더링 확인
→ 물리 기기 대기(PENDING)

---

## 전수 검사 — 안전장치 보강 (릴리즈 빌드 기준)

**C-001** [!] 치명 결함 — 센서 없는 폰에서 가짜 골든값이 진짜 리포트로 저장
→ 원인: 센서 미가용 시 mock 스트림 폴백 + 샘플 부족 시 골든값 복사 저장
→ 해결: 해당 저장 경로 삭제, 측정 시작 화면에서 `getDefaultSensor` 사전 체크로 미지원 기기 경고·버튼 비활성, mock 폴백은 디버그 모드로 격리

**C-002** [O] 이메일 첨부 실패 — 저장 경로와 FileProvider 허가 목록 불일치
→ 해결: file_paths.xml에 root-path 추가

**C-003** [O] 프라이버시 — 측정 중 백그라운드 전환 시 마이크 계속 녹음
→ 해결: `WidgetsBindingObserver`로 앱 생명주기(paused) 감지 → 캡처 자동 중단·재측정 안내

**C-004** [O] 데이터 품질 — 안 움직인 승강기도 정상 리포트로 저장
→ 해결: 저장 전 타당성 게이트(측정시간·속도·거리 미달 시 경고 다이얼로그), 임계값은 MetricsConfig에 정의

**C-005** [O] 소소한 보완
→ 라이브 속도 부호유지 적분으로 하강 시 0고착 수정, 저장 재시도 버튼, 마이크 권한 영구거부 안내

**C-006** [!] 재시도 버튼 테스트가 다이얼로그 애니메이션 교착으로 1시간 무한반복
→ 문제: AI가 같은 접근만 변주, 테스트 통과용 인위적 지연을 실제 코드에 삽입
→ 해결: 방향 전환 — 저장 로직을 순수함수로 분리해 유닛테스트, UI는 1회 노출만 검증, 인위적 지연 원복

**C-007** [O] 진행 방식
→ 한 스텝=한 커밋 8회 분할로 누락 방지, 테스트 41→54건

---

## 리팩토링·문서화

**D2-001** [O] 리팩토링 진단
→ 도메인 계층은 양호, UI 계층 부채 식별(180줄 함수·중복 위젯 18곳·분산된 저장호출)
→ 검증 로직 보존 원칙으로 5묶음 순차 정리 계획 수립

**D2-002** [O] 서류·인수인계용 문서화
→ 통합계획서·연구노트(실패과정 보존)·전체 코드흐름·단계별 설명·README·개발일지 작성

---

## 실기기 1차 테스트 — 안정화·진단 (커밋 3f9d4a4)

**S-001** [!] 정속 구간 시간이 106만 초로 비정상 표시
→ 원인: Android 센서 timestamp가 앱 측정 시작이 아닌 기기 내부(부팅) 기준
→ 해결: 측정 시작 기준 상대 시간으로 변환
→ 결과: "2.3초 ~ 22.4초"처럼 정상 표시. 골든 시뮬레이션에선 안 드러나고 실기기에서만 터지는 유형

**S-002** [O] 거리/속도 계산 소스를 raw−gravity 모션 성분으로 분리
→ 기술: Kotlin에서 TYPE_LINEAR_ACCELERATION + TYPE_ACCELEROMETER + TYPE_GRAVITY 3종 동시 구독, raw/gravity 값을 EventChannel로 함께 전달
→ 구현: 진동=linear acceleration, 거리/속도=raw−gravity로 파이프라인 분리 (진동은 짧고 빠른 흔들림, 거리/속도는 큰 이동 흐름 — 필요한 성분이 다름)
→ 결과: Lift Check(거리 32.48m / 속도 1.52m/s) 대비 우리 앱 거리 29~37m / 속도 1.46~1.70m/s. 과거 비정상(0.4~1.6m) 완전 탈출, 속도는 거의 일치

**S-003** [O] 단위 변환 검증 — 진동 과대 원인 후보에서 공식 제외
→ 검증: Kotlin MPS2_TO_MG(101.9716...) ↔ Dart mgToMetersPerSecondSquared(0.00980665) 상수·계산식 대조
→ 결과: 단위 변환 정상. 진동값 과대의 원인이 아님을 확정

**S-004** [O] 정속 구간 진단 박스 추가 (결과 화면)
→ 구현: 정속 시작/종료 시간, 자동 검출 여부, 샘플 수, 전체 대비 비율, 전체/정속 P-P 표시
→ 목적: 진동 과대가 출발/정지 구간 탓인지 정속 구간에서도 큰지 구분

**S-005** [O] 필터 전/후 P-P 비교 기능 추가 (결과 화면)
→ 구현: 축별로 필터 전(raw) / 후(필터 출력) P-P를 나란히 표시
→ 결과: 필터가 어떤 축에서 얼마나 영향을 주는지 즉시 확인 가능. 이후 필터 재설계(E단계) 검증의 핵심 도구가 됨

**S-006** [△] 진동 필터 1차 — high-pass 적용
→ 시도: 출발/정지 가속·기울어짐·저주파 이동 성분 제거 목적으로 HP 0.5/0.5/1.0Hz 적용
→ 결과: Z 일부 감소, X/Y는 미미 (당시 X 17~18 / Y 27~28 / Z 41~43mg). HP만으로는 불충분 판단

**S-007** [!] 진동 필터 2차 — 이동평균 band-limit(LP 10Hz 추가), 사후 회귀 판명
→ 시도: Lift Check PDF의 ISO 8100-34 가중 필터 언급을 근거로 HP+LP 10Hz 구조 적용. 실기기 값 X 6.5~8.6 / Y 8.8~12.4 / Z 16.9~21.1mg 달성, "Lift Check 근접"으로 판단하고 커밋
→ 문제(사후 규명): ① 커밋 전 검증이 analyze+build뿐, flutter test 누락 → 골든 Z 회귀(22.5→9.9mg)가 미검출 통과 ② 비교 기준 오류 — 우리 표시값(A95)을 Lift Check max P-P와 비교. A95끼리 비교하면 여전히 2~3배 높은 상태였음
→ 결과: E-001~E-003의 회귀 규명 및 필터 재설계로 이어짐

**S-008** [O] 측정 종료 멈춤 수정
→ 문제: 테스트 완료 후 "대기중..." 고착 — 센서 구독 종료/stopCapture 지연이 원인
→ 해결: 종료 중 중복클릭 방지, "종료 중..." 표시, stopCapture 타임아웃(초과 시 무시하고 분석·저장·화면 이동 진행)

**S-009** [△] 기준 초과 임계값(X/Y 10, Z 15mg)은 검증용 임시 유지
→ 원칙: 파이프라인 검증 중 기준값을 임의 조정해 정상처럼 보이게 하지 않음
→ 확정 근거는 이후 확보: Lift Check·기존 앱·반복측정·복수 엘리베이터·OTIS 내부 기준

---

## 통합 7단계 — 진동 필터 재설계 (biquad IIR + ISO 8041 Wd)

**E-001** [!] 이동평균 band-limit 필터의 회귀 확인
→ 시도: 커밋 3f9d4a4(이동평균 HP 0.5/0.5/1.0Hz + LP 10Hz)가 골든 Z Aptp를 22.5→9.9mg로 떨어뜨린 회귀를 재현
→ 구현: 코드 변경 없이 flutter test로 실측값만 기록
→ 결과: Z Aptp 9.94mg, closeTo(24.0, 4.0) 단언 실패 확인. 원인은 이동평균의 실제 -3dB 컷오프가 명목값과 다름(27샘플 LP의 실효 -3dB ≈ 4.2Hz)으로 특정 — Z축 승차감 대역(4~25Hz)이 통째로 제거되고 있었음

**E-002** [O] 필터 인프라를 이동평균 → biquad IIR로 교체 (사양 변경 없이 구현체만 교체)
→ 시도: 2차 Butterworth biquad(Direct Form II Transposed)를 순수 Dart로 구현해 진동 경로만 교체, motion(거리/속도) 경로의 이동평균은 그대로 유지
→ 구현: RBJ Cookbook 형태의 bilinear 변환(Q=1/√2, 주파수 사전왜곡)으로 HP/LP 계수 산출, 계수는 실측 샘플레이트로 매 분석 시 재계산, Nyquist 안전 가드(0.45×sampleRate) 추가, 인과(causal) 적용
→ 결과: 골든 X 20.10 / Y 26.98 / Z 19.88mg — 사전 오프라인 시뮬레이션 예측과 소수점 둘째 자리까지 일치(구현 정확성 입증). 컷오프 미변경 단계라 골든 단언 통과는 목표 아니었음. baseline 대비 신규 회귀 없음

**E-003** [O] 축별 필터 사양 확정: Z=Butterworth band-limit, X/Y=ISO 8041 Wd
→ 시도: 골든 픽스처 오프라인 실험으로 사양을 먼저 확정 — X/Y에 ISO 8041 Wd(수평 진동 가중) 적용 시 정답에 오차 0.03~0.26mg으로 수렴, Z는 Butterworth 1.0~30Hz가 정답 일치
→ 구현: Z축 HP 1.0Hz + LP 30.0Hz Butterworth, X/Y축 Wd 캐스케이드(대역제한 HP 0.4Hz + LP 100Hz + 가속도-속도 천이 f=2.0Hz, Q=0.63). MetricsConfig에 VibrationFilterType enum과 Wd 상수 단일 정의(출처 ISO 8041 Wd 명시), 천이 필터용 일반 2차 아날로그 전달함수 bilinear 변환 함수 신규 작성(3개 샘플레이트 계수 검산, 차이 ≤3e-16)
→ 결과: 골든 X 8.46 / Y 12.88 / Z 22.45mg — 3축 모두 강화된 허용오차(X 8.2±1.0, Y 12.9±1.0, Z 22.2±1.5)를 동시 통과. 골든 단언도 느슨한 검증(X>0, Y>X, Z 24±4)에서 위 수치로 조임. noiseMax/distance/maxSpeed 단언 계속 통과. B1-006에서 이월된 X/Y 과대산출(OI-1)의 골든 기준 해소

**E-004** [O] 주석·문서 정리 및 3종 검증 게이트 확정
→ 시도: vibration_metrics.dart의 stale stride 주석(1/5 → 실제 1/10) 수정, metrics_config.dart/signal_filters.dart의 이동평균 기반 설명을 신규 필터 아키텍처 설명으로 갱신, 필터 전/후 P-P 비교 기능의 신규 필터 반영 확인
→ 구현: 문서 주석만 수정(로직 변경 없음), 골든 픽스처를 엔진에 직접 통과시켜 전/후 값 검증
→ 결과: X 23.33→8.56mg, Y 32.58→12.88mg, Z 90.79→22.45mg로 비교 기능 정상 반영 확인. flutter analyze/test/build apk --debug 3종을 이후 모든 커밋 전 필수 게이트로 확정

**E-005** [O] 기지(旣知) 실패 세트 공식화 — 테스트 게이트 기준 확정
→ 시도: 필터 작업 이전부터 존재한 테스트 실패 11건을 baseline(변경 전 커밋) 대조로 분리해, 신규 회귀와 구분하는 공식 목록 등록
→ 구현: T-1(parse_raw_test 1건 — 4샘플 raw가 엔진 전체 파이프라인을 타는 테스트 설계 결함, 재설계 필요), T-2(widget_test 10건 — 다이얼로그 타이머 pending, C-006 계열)
→ 결과: 게이트 기준 확정 — "T-1/T-2 제외 신규 실패 0건". 세트에 새 항목을 임의 추가(새 실패를 기지로 편입)하는 것은 승인 없이 금지

---

## 최종 이월 항목 (코드로 불가, 현장·확정 필요)

**X-001** [P] 실기기 샘플레이트 256Hz + 마이크 dBA 반응 실측

**X-002** [P] 실기기 이메일 첨부 실제 렌더링 확인

**X-003** [△] OI-1 X/Y 진동 Aptp 캘리브레이션
→ 골든(EVA) 기준은 ISO 8041 Wd 적용으로 해소(E-003). 남은 것: 실기기 동시측정 기반 기기별 캘리브레이션

**X-004** [ ] OI-2 Login ID 관리 방향 확정 (서버 인증 연동)

**X-005** [ ] OI-3 협력사 휴대폰 기종 조사

**X-006** [ ] OI-4 소음 dBA 기기별 오프셋 캘리브레이션 (임시치 +85 대체)

**X-007** [ ] Lift Check 동시측정으로 제2 골든 픽스처 확보
→ 같은 엘리베이터·같은 주행의 우리 앱 raw(EVIMP1) + Lift Check 리포트 짝. 확보 후 캘리브레이션은 재탑승 없이 오프라인 리플레이로 진행. 비교는 A95↔A95, max P-P↔max P-P로만

**X-008** [ ] T-1 parse_raw_test 재설계 (기지 실패 해소)

**X-009** [ ] T-2 다이얼로그 타이머 pending 해소 (UI 작업 재개 시, 기지 실패 해소)
