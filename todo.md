# 다음 주 실측정 계획 및 엔지니어 작업 목록

> 목적: 재설계된 필터(X/Y: ISO 8041 Wd, Z: Butterworth 1.0~30Hz)를 실기기에서
> 검증하고, 이후 캘리브레이션의 기반이 될 **동시 측정 데이터(제2 정답지)**를
> 확보한다.
>
> 작성: 2026-07-11

---

## A. 배경: 지난주 무슨 문제가 있었고, 어떻게 고쳤나

이번 측정 계획이 왜 이런 모양인지 이해하려면 지난주 사건을 알아야 한다.

### 문제: 실기기 값을 좋게 만들다가 정답 계산을 깨뜨렸다

실기기 진동값이 Lift Check보다 크게 나와서, 고주파를 잘라내는 필터(low-pass
10Hz)를 추가했다. 실기기 값이 내려와서 "Lift Check에 가까워졌다"고 판단하고
커밋했다. 그런데 사실은:

1. **필터가 이름값을 못 했다.** 이동평균 방식으로 만든 "10Hz 필터"의 실제 차단
   지점은 약 4.2Hz였다. 라벨은 10인데 실물은 4인 자를 들고 튜닝하고 있던 셈이다.
2. **진짜 신호까지 잘라냈다.** 엘리베이터 수직 진동의 핵심 대역(4~25Hz)이 통째로
   제거되어, 정답지(실제 OTIS 장비 측정 데이터, 정답 X 8.2 / Y 12.9 / Z 22.2mg)를
   넣으면 Z가 9.9mg — 정답의 절반 이하로 나오는 상태가 됐다.
3. **자동 채점을 안 돌리고 커밋했다.** 이 회귀를 잡아줄 골든 테스트가 이미
   존재했는데, 커밋 전 검증에서 `flutter test`가 빠져 있었다. 관문은 있었는데
   아무도 통과하지 않은 것이다.
4. **채점 기준도 어긋나 있었다.** 우리 앱 표시값(A95)을 Lift Check의 다른 지표
   (max P-P)와 비교해 "거의 비슷하다"고 읽었다. 자기에게 유리한 기준을 골라
   비교하면 개선이 실제보다 커 보인다.

요약하면: **값이 목표에 "가까워 보이는 것"과 계산이 "맞는 것"을 혼동했다.**
신호를 깎아서 목표 숫자에 붙이는 것은, 기준값을 낮춰서 정상처럼 보이게 하는 것과
같은 종류의 자기기만이다.

### 해결: 코드보다 검증을 먼저 했다

1. 코드를 고치기 전에 **정답지로 오프라인 실험**을 먼저 해서, 어떤 필터 구성이
   정답에 수렴하는지 수치로 확정했다 (X/Y는 국제 표준 ISO 8041의 수평 진동
   가중 필터, Z는 제대로 만든 Butterworth 1.0~30Hz 대역 필터).
2. 확정된 사양으로 4단계 나눠 구현했고, 매 단계 결과를 독립 계산과 대조했다.
3. 최종 결과: 정답지 3축 동시 통과 (X 8.46 / Y 12.88 / Z 22.45 vs 정답
   8.2 / 12.9 / 22.2). 자동 채점(골든 테스트)의 허용 오차도 ±4.0에서 ±1.0~1.5로
   조였다 — 다음에 비슷한 회귀가 생기면 더 빨리, 더 확실하게 걸린다.

### 앞으로의 작업 원칙 (이런 일이 다시 없으려면)

| # | 원칙 | 왜 |
|---|---|---|
| 1 | **정답지가 항상 우선이다.** 실기기 값이 어떻게 나오든, 정답지 계산이 깨지는 변경은 하지 않는다 | 실기기 값에는 기기·거치·환경 변수가 섞여 있지만, 정답지는 계산 로직만 순수하게 검증한다. 정답지가 깨졌다는 것은 계산 자체가 틀렸다는 뜻 |
| 2 | **커밋 전 3종 검증 필수**: analyze / **test** / build. test 통과 기준은 "기지 실패 세트(T-1/T-2) 외 신규 실패 0건" | 이번 회귀는 test 한 번만 돌렸어도 커밋 전에 걸렸다. 관문은 통과할 때만 관문이다 |
| 3 | **비교는 같은 지표끼리만.** A95는 A95와, max P-P는 max P-P와 | 유리한 기준을 골라 비교하면 판단이 왜곡된다. 비교표 양식(4장)에 짝을 박아둔 이유 |
| 4 | **값을 바꾸는 수정 전에는 예상 수치를 먼저 적는다.** "이 변경으로 정답지 값이 어떻게 될 것" → 실행 → 대조 | 예상 없이 실행하면 어떤 결과든 사후 합리화가 가능해진다. 예상과 다르면 그게 버그 신호다 |
| 5 | **현장에서는 데이터만 가져온다.** 필터·설정·기준값 변경은 사무실에서 raw 리플레이로 | 현장 즉흥 수정은 "왜 바꿨는지"가 기록에 안 남고, 재현도 안 된다. raw만 있으면 모든 실험은 책상에서 가능하다 |
| 6 | **미달은 미달이라고 쓴다.** 보고서에 "사실상 통과", "거의 비슷" 같은 표현으로 미달을 덮지 않는다 | 이번에 에이전트가 "전체 스위트 통과: 미달"을 정직하게 보고하면서 원인 분리가 가능했다. 정직한 미달 보고는 실패가 아니라 검증 체계가 작동한다는 증거다 |

이 원칙들은 이번 측정에만 적용되는 게 아니라, 이후 캘리브레이션과 기준값(OI-2)
확정 작업 전체의 기본 규칙이다.

---

## 0. 가장 중요한 사전 인지 사항 (측정 전에 반드시 읽을 것)

### 0-1. Z값이 이전보다 오르면 그것이 정상이다
이전 측정(Z 16.9~21.1mg)은 실질 4.2Hz까지 잘라내는 과도한 필터의 결과였고,
그 필터는 정답지(골든 픽스처) 검증에서 Z를 반토막 내는 회귀로 판명되어 폐기됐다.
새 필터에서는 Z에 30Hz까지의 실제 진동이 포함되므로 **수치가 오른다.
"값이 올랐으니 필터를 다시 조이자"는 판단은 금지** — 그것이 지난주 회귀의
재발이다. 판정은 아래 0-2의 비교 기준으로만 한다.

### 0-2. 비교는 반드시 같은 지표끼리
우리 앱 메인 표시값은 **A95(Aptp)**다. Lift Check 비교 시:

| 우리 앱 | Lift Check 비교 대상 (참고 PDF 기준) |
|---|---|
| 메인 표시값 (A95) | A95: X 3.76 / Y 3.66 / Z 12.97 mg |
| 진단 박스의 정속 P-P | constant max P-P: Z 16.43 mg |
| 진단 박스의 전체 P-P | max P-P: X 8.68 / Y 6.43 mg |

우리 A95를 상대 max P-P에 붙여 읽는 순간 결과 해석이 왜곡된다.

### 0-3. 폰 거치 방향 결정 (미해결 쟁점, 측정 전 필수 결정)
현장 안내 문서(FOD FI-2026-697-033)와 개발 요구사항 PPT의 폰 배치 방향이
상충하는 쟁점이 아직 미해소 상태다. X/Y 축 매핑이 방향에 따라 뒤바뀌므로,
**이번 측정에서 어느 방향으로 놓을지 사전에 하나로 정하고, 모든 측정에서
동일하게 유지하며, 기록지에 방향을 남긴다.** (쟁점의 최종 해소는 별도 트랙으로
진행하되, 측정 데이터가 방향 불명이 되는 것만은 막아야 한다.)

---

## 1. 사전 준비 (측정일 이전, 사무실에서)

| # | 항목 | 세부 |
|---|---|---|
| 1 | 최신 develop 빌드 설치 | 필터 재설계 커밋 포함 여부 확인: 설정 또는 로그에서 골든 검증값(X 8.46 / Y 12.88 / Z 22.45) 재현되는 빌드인지 확인 |
| 2 | 측정 기기 준비 | Galaxy S22 (기준 기기), 배터리 충분, 방해금지 모드 |
| 3 | Lift Check 기기 준비 | iOS 기기 + Lift Check 설치, PDF 리포트 내보내기 방법 숙지 |
| 4 | 기준 소음계 (가능 시) | 소음 dBA 오프셋(+85 임시치) 실측 캘리브레이션용. 없으면 이번 주는 진동 우선, 소음은 다음 기회 |
| 5 | raw 저장 동작 확인 | 사무실에서 테스트 측정 1회 → EVIMP1 raw 파일이 저장되고 이메일 첨부로 회수 가능한지 확인. **현장 가서 저장 안 되는 것을 발견하면 하루를 버린다** |
| 6 | 기록지 준비 | 아래 5번 양식 인쇄 또는 메모앱 준비 |

---

## 2. 측정 프로토콜 (현장)

### 2-1. 배치
- 두 기기(S22 + Lift Check 기기)를 **엘리베이터 바닥 중앙 부근에 나란히** 놓는다.
  같은 바닥, 같은 방향(0-3에서 정한 방향), 서로 닿지 않게.
- 케이스는 두꺼운 충격흡수형이면 제거(바닥 접촉 특성이 달라짐). 어떤 상태로
  쟀는지 기록.
- 측정 중 탑승자는 움직이지 않는다. 폰을 손에 들거나 주머니에 넣지 않는다.

### 2-2. 절차 (1회 기준)
1. 두 앱 모두 측정 대기 상태로 준비
2. 우리 앱 측정 시작 (5초 카운트다운 동안 손 떼고 물러남)
3. Lift Check 측정 시작
4. 엘리베이터 주행 (최하층 → 최상층 또는 역방향, 논스톱)
5. 정지 후 2~3초 대기 → 양쪽 측정 종료
6. 우리 앱: 결과 화면의 **메인 값(A95) + 진단 박스(정속 구간 시간·비율,
   전체/정속 P-P, 필터 전/후)** 를 기록지에 옮김. raw 저장 확인
7. Lift Check: 리포트 저장(PDF)

### 2-3. 반복 횟수 (최소)
| 구분 | 횟수 |
|---|---|
| 상행 (하부 → 상부) | 3회 이상 |
| 하행 (상부 → 하부) | 3회 이상 |
| (여유 시) 다른 엘리베이터 1대 추가 | 상행 2회 |

반복이 필요한 이유: 단발 측정으로는 값의 산포(재현성)를 알 수 없고,
산포를 모르면 이후 기준값(OI-2 정확도 허용오차) 논의가 불가능하다.

### 2-4. 파일명/기록 규칙
- raw 파일과 기록지에 회차 번호를 일치시킨다. 예: `현장명_호기_UP_01`
- 하나라도 조건이 다르면(문 열림, 중간 정차, 사람 탑승 등) 기록지에 비고로 남기고
  가능하면 해당 회차는 재측정.

---

## 3. 현장 판정 가이드 (즉석 확인용)

현장에서 아래만 빠르게 확인한다. 정밀 분석은 사무실 오프라인에서.

| 확인 | 정상 기준 | 비정상 시 |
|---|---|---|
| 정속 구간 자동 검출 | "검출됨", 비율 30% 이상 수준 | 미검출 반복 시 주행 시간이 8초 이상인지 확인 |
| 거리 | 층수 × 층고 근사치 | 2배 이상 벗어나면 비고 기록 후 재측정 |
| 속도 | Lift Check와 ±0.1m/s 수준 | 크게 다르면 비고 기록 |
| Z ≥ X, Y 경향 | 수직 이동체이므로 Z가 대체로 큼 | X나 Y가 Z보다 지속적으로 크면 거치 방향 재확인 |
| raw 저장 | 매 회차 저장 확인 | 저장 실패 시 해당 회차 무효, 재측정 |

**현장에서 하지 말 것**: 값이 마음에 안 든다고 필터·기준값·설정을 현장에서
바꾸는 것. 데이터만 가져온다.

---

## 4. 측정 후 작업 (사무실, 엔지니어)

| # | 작업 | 산출물 |
|---|---|---|
| 1 | raw EVIMP1 파일 전량 회수 (이메일 첨부) | 회차별 raw 파일 |
| 2 | 회차별 결과를 비교표로 정리 (아래 양식) | 비교표 |
| 3 | 산포 확인: 같은 방향 반복 측정의 A95 최대-최소 차이 | 재현성 수치 |
| 4 | 대표 1회차 raw를 **제2 정답지 후보**로 지정 — Lift Check PDF와 짝을 이루는 회차 | 제2 골든 픽스처 후보 + 대응 Lift Check 리포트 |
| 5 | 오프라인 리플레이: 제2 정답지를 엔진에 넣고 값 재현 확인 (재탑승 없이 이후 튜닝 가능해짐) | 리플레이 검증 기록 |
| 6 | 연구노트에 결과 기록 (성공/미달 그대로) | 연구노트 항목 |

### 비교표 양식 (회차별)
```
회차: ____ (방향: 상행/하행, 거치 방향: ____, 비고: ____)

지표            우리 앱      Lift Check    비고
X A95           ___ mg      ___ mg        A95끼리 비교
Y A95           ___ mg      ___ mg        A95끼리 비교
Z A95           ___ mg      ___ mg        A95끼리 비교
X max P-P       ___ mg      ___ mg        (진단 박스 전체 P-P)
Y max P-P       ___ mg      ___ mg
Z 정속 P-P      ___ mg      ___ mg        (constant max끼리)
거리            ___ m       ___ m
최대속도        ___ m/s     ___ m/s
소음 max        ___ dBA     ___ dBA       오프셋 미캘리브레이션 참고치
정속 비율       ___ %       —
```

---

## 5. 판정 기준 (측정 결과를 어떻게 읽을 것인가)

1. **거리/속도**: Lift Check 대비 속도 ±0.1m/s, 거리 ±15% 수준이면 합격권.
2. **진동 A95 비율**: 우리 A95 ÷ Lift Check A95를 축별로 산출. 이 비율이
   회차 간 **일정하게 유지되는지**가 핵심 관찰 포인트다.
   - 비율이 일정 → 기기/거치 특성에 의한 체계적 차이. 오프라인에서 보정 계수
     또는 필터 미세조정으로 흡수 가능 (제2 정답지로 리플레이 튜닝)
   - 비율이 들쑥날쑥 → 거치·바닥 접촉 등 측정 조건 문제. 필터를 건드리기 전에
     측정 방법부터 통제
3. **기준 초과 UI(X/Y 10mg, Z 15mg)는 여전히 임시 참고값.** 이번 측정으로
   빨강이 떠도 판정에 사용하지 않는다. 기준값 확정은 반복 측정 산포 +
   OTIS 내부 기준 확보 이후(OI-2).

---

## 6. 엔지니어 작업 목록 종합 (우선순위순)

### 이번 주 필수
- [ ] 사전 준비 1~6 (특히 raw 저장 사무실 검증)
- [ ] 폰 거치 방향 단일 결정 및 전 회차 통일
- [ ] 동시 측정 상행 3회 + 하행 3회 이상, 기록지 완성
- [ ] raw 파일 회수 및 비교표 작성
- [ ] 제2 정답지 후보 지정 + 오프라인 리플레이 재현 확인

### 이번 주 가능 시
- [ ] 기준 소음계 동시 측정으로 dBA 오프셋 실측 (임시치 +85 대체 근거 확보)
- [ ] 다른 엘리베이터 1대 추가 측정 (기종 간 차이 데이터)

### 별도 트랙 (측정과 병행, 급하지 않음)
- [ ] 거치 방향 문서 상충(FOD FI-2026-697-033 ↔ 요구사항 PPT) 공식 해소 —
      배치 안내 화면(S3) 확정의 선행 조건
- [ ] 기지 실패 T-1(parse_raw_test 재설계), T-2(다이얼로그 타이머) —
      UI 작업 재개 시 처리
- [ ] 비교표 결과 기반으로 기준값(OI-2) 논의 시작

### 금지 사항 (재확인)
- 현장에서 필터·설정·기준값 변경
- Z값 상승을 회귀로 오판하여 필터를 조이는 것
- A95를 max P-P와 비교하여 결론 내리는 것
- 커밋 시 3종 검증(analyze / test / build) 생략 — test 게이트 기준은
  "기지 실패 세트(T-1/T-2) 외 신규 실패 0건"
